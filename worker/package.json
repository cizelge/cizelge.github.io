{
  "name": "cizelge-oy",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute cizelge-oy --local --file=schema.sql",
    "db:remote": "wrangler d1 execute cizelge-oy --remote --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260915.1",
    "wrangler": "^4.34.0"
  }
}
